export const authSelector = (state) => state.auth;

export const alertSelector = (state) => state.alert;

export const themSelector = (state) => state.theme;

export const activePageSelector = (state) => state.activePage;

export const userSelector = (state) => state.users;

export const postSelector = (state) => state.homePost;

export const profileSelector = (state) => state.profile;

export const statusSelector = (state) => state.status;

export const detailPostListSelector = (state) => state.detailPostList;
